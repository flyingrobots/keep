[package]
name = "xtask"
version = "0.0.0"
edition = "2024"
rust-version = "1.96"
publish = false

[dependencies]
blake3 = { version = "=1.8.5", default-features = false, features = ["pure", "std"] }

[lints]
workspace = true
