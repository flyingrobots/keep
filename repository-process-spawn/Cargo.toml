[package]
name = "repository-process-spawn"
version = "0.0.0"
edition = "2024"
rust-version = "1.96"
license = "Apache-2.0"
publish = false

[dependencies]
rustix = { version = "=1.1.4", default-features = false, features = ["process", "std"] }

[lints]
workspace = true
